import Link from 'next/link'

import { ChevronDownIcon } from '@radix-ui/react-icons'
import { PackageManager } from '@components/typewriter'



export function SmoothScroll({ scrollTo, children, ...props }) {return (
  <button {...props} onClick={() => document.getElementById(scrollTo)?.scrollIntoView({ behavior: 'smooth' })} >
    {children}
  </button>
)}

export function Feature({ title, description, ...props }) {return (
  <div {...props} className='flex flex-col gap-2 text-center border bg-neutral-200/90 dark:bg-neutral-800/90 rounded px-4 py-6'>
    <p className='nx-mt-0 text-3xl font-bold'>{title}</p>
    <p className='nx-mt-0'>{description}</p>
  </div>
)}




<div className="flex-col flex">

  {/* Section 1 */}
  <section id='top'>
    <div className='h-full justify-center pb-16 text-center items-center flex flex-col gap-4' style={{ height: 'calc(100vh - 64px)' }}>
      <h1 className='text-4xl font-bold'><span className='bg-clip-text text-transparent bg-gradient-to-r from-pink-500 to-violet-500'>Thread</span> - v1.0.0</h1>
      <div className="text-center"><PackageManager /></div>
      <Link
        href='/docs'
        className='block mt-12 transition-all bg-neutral-800 dark:bg-neutral-200 text-white dark:text-black hover:bg-neutral-800/90 dark:hover:bg-neutral-200/90 active:bg-neutral-800/80 dark:active:bg-neutral-200/80 px-4 py-2 rounded'
      >
        Get Started
      </Link>
    </div>
    <SmoothScroll scrollTo='features' className='absolute flex left-1/2 -translate-x-1/2 items-center justify-center' style={{ top: 'calc(100vh - 64px)' }}>
      <ChevronDownIcon className='w-8 h-8' />
    </SmoothScroll>
  </section>


  {/* Section 2 */}
  <section id='features'>
    <div className='py-4 flex flex-col gap-4 justify-center items-center' style={{ height: 'calc(100vh - 64px)' }}>
      <h2 className='text-5xl font-bold mb-10 text-center'>Features</h2>
      <div className='flex flex-wrap gap-8'>
        <Feature title='Graceful shutdown' description='Placeholder' />
        <Feature title='Data Processing' description='Placeholder' />
      </div>
    </div>
  </section>

</div>

